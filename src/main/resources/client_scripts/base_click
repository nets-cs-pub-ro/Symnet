source :: FromNetFront(MACADDR)

filter :: IPFilter(deny tcp && dst port 8080, allow udp && dst 10.0.0.1 && dst port 80, -)

rewriter :: IPRewriter(pattern - - 0.0.0.1 - 0 0)

dest :: ToNetFront(MACADDR)

source -> filter -> rewriter  -> SimpleQueue -> dest
